# https://www.acmicpc.net/problem/3052
# 나머지 (수학, 사칙연산) / 브론즈2

lst = []
new_lst = []

for i in range(10):
  lst.append(int(input())%42)

for j in lst:
  if j not in new_lst:
    new_lst.append(j)

print(len(new_lst))

# 파이썬 리스트 중복 제거 방법, 리스트 len(list이름)함수 이용
# set을 이용하여 리스트 중복 제거도 가능 (list -> set -> list)
